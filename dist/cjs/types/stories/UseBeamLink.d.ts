import React from "react";
export interface UseBeamLinkProps {
    linkToken: string;
}
/**
 * Primary UI component for user interaction
 */
export declare const UseBeamLink: ({ linkToken }: UseBeamLinkProps) => React.JSX.Element;
